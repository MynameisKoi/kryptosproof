version: "3.8"

services:
  # Deliberately vulnerable web app — red team target
  dvwa:
    image: vulnerables/web-dvwa
    ports:
      - "8080:80"
    networks:
      - sandbox
    environment:
      - RECAPTCHA_PRIV_KEY=
      - RECAPTCHA_PUB_KEY=

  # Main kryptosproof platform (frontend + backend in one image)
  kryptosproof:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - dvwa
    networks:
      - sandbox
    environment:
      - PORT=3000
      - TARGET_URL=http://dvwa:80
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - AI_MODEL=${AI_MODEL}
    volumes:
      - ./logs:/app/logs

networks:
  sandbox:
    driver: bridge
    name: kryptosproof_sandbox
